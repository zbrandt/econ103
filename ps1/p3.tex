\begin{homeworkProblem}
    Let \(f : \mathbb{R}^2 \to \mathbb{R}\) be a twice-differentiable function. Show that \(\frac{\partial^2}{\partial x \partial y} f \geq 0\) if and
    only if for every \(x, x', y, y' \in \mathbb{R}\):

    \[
        x' > x \text{ and } y' > y \Rightarrow f(x', y') - f(x, y') \geq f(x', y) - f(x, y).
    \]

    \solution

    % Your solution here

\end{homeworkProblem}
